#!/bin/sh

# Duplicated to +/tmux-connect ~/Unixhome/bin/zzextensions/ssh-extensions/bin/ssht
if tmux list-sessions | grep -vq ' (attached)$'; then
    exec tmux-wrapper attach-session \; choose-tree -s
else
    exec tmux-wrapper new-session \; choose-tree -s
fi
